# 发布到 GitHub（一次 3 分钟，之后同事永久一句话安装）

> **✅ 已发布（2026-09-15）**：https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill
> 仓库为公开、默认分支 `main`，根目录直接是 `SKILL.md` / `scripts` / `references`（14 个文件，已逐字节比对一致）。
> 本次因本机 C 盘写满，**改用 GitHub API 直传**（`push_api.py`），本地 git 的 6 个提交与远端历史不相关 ——
> 所以**以后更新请重跑 `python push_api.py <PAT>`**，不要直接 `git push`（会被拒）。

本地仓库已经建好了，就在：

```
C:\Users\Administrator\WorkBuddy\2026-09-14-13-42-10\howie-intl-cn2en-listing-skill
```

已提交 14 个文件（`SKILL.md` + 6 个脚本 + 翻译手册 + README + 安装器 + 发布脚本 + 本文件），只差推到 GitHub。
下面三种方式**任选一种**，做完同事就能用了。

> 你的账号是刚注册的（`Howie-Linhao`，今天创建）。**先确认邮箱里的验证链接已经点过**，
> 未验证的账号 GitHub 会限制建仓库和推送。

---

## 方式 1 · 网页上传（不用敲命令，最简单）

1. 打开 https://github.com/new
2. **Repository name** 填 `howie-intl-cn2en-listing-skill`
3. 选 **Public**（同事不用登录也能装；想私有也行，同事有权限即可）
4. **不要**勾 "Add a README file"（我们已经有 README 了）
5. 点 **Create repository**
6. 新页面点 **uploading an existing file**
7. 打开本地文件夹 `howie-intl-cn2en-listing-skill`，**全选里面的所有内容**（包括 `scripts`、`references` 两个文件夹和隐藏的 `.gitattributes`），**整批拖进上传框**
   > 注意拖的是**文件夹里面的东西**，不要把 `howie-intl-cn2en-listing-skill` 这个壳一起拖进去，
   > 否则仓库里会多一层目录，同事 clone 下来路径就不对了。
8. 点 **Commit changes** 完成

---

## 方式 2 · 命令行（仓库已 init + commit，只剩 3 条）

先按方式 1 的第 1–5 步建一个**空仓库**（不勾 README），然后：

```bash
cd "C:\Users\Administrator\WorkBuddy\2026-09-14-13-42-10\howie-intl-cn2en-listing-skill"
git remote add origin https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill.git
git push -u origin main
```

首次推送会要求登录。**注意：GitHub 现在不接受账号密码**，弹出提示时：
用户名填 `Howie-Linhao`，**密码栏粘贴 PAT**（PAT 怎么领见方式 3 第 1 步）。

> 如果建仓库时不小心勾了 README，先执行 `git pull --rebase origin main` 再 push。
> 这条路线对新手容易卡在登录上 —— 拿不准就直接走方式 3，或干脆用方式 1 纯网页。

---

## 方式 3 · 一条命令（建仓库 + 推送 + 自检，推荐）

1. 领 PAT：https://github.com/settings/tokens?type=beta → **Generate new token**
   - Repository access: **All repositories**
   - Permissions → Repository permissions → **Contents: Read and write**
   - 有效期 7 天
2. 执行：

```bash
cd "C:\Users\Administrator\WorkBuddy\2026-09-14-13-42-10"
python 发布.py --token <粘贴你的PAT>
```

脚本会：建仓库 → 推送 → 把 `origin` 配成干净 URL（**token 不写进 config**）→ 复查仓库可达 → 打印给同事的那句话。
失败会直接告诉你原因（401 无效 / 403 权限不足 / 422 已存在）。
**跑完立刻去 https://github.com/settings/tokens 把这个 token 删掉。**

也可以把 token 发我，我代跑。

---

## 发布后必做的一次自检（这步漏了同事就是白装）

**光有 URL 不等于仓库存在。** 我之前给的那条链接就踩过这个坑：URL 格式对，但仓库压根没建，同事照着装 —— 助手取不到技能，只能自由发挥，出来的文档跟你的模板完全不一样。

发布完在浏览器打开这个链接，**看到仓库页面（不是 404）才算成功**：

```
https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill
```

或者命令行：

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill
# 200 = 通了，可以发给同事；404 = 还没发布，别发
```

> 若是私有仓库，未登录访问也会显示 404，此时要确认同事有该仓库的访问权限。

## 同事装完也要自检（三招）

1. 让他问助手：**「列出你本机 `.workbuddy/skills` 目录下有哪些技能」** —— 必须看到 `intl-cn2en-listing`；
2. 让他看路径 `C:\Users\<用户名>\.workbuddy\skills\intl-cn2en-listing\SKILL.md` 是否存在（**只有一个 SKILL.md、没有 scripts / references 就是没装好**）；
3. 触发一次：说「用国际站速卖通中转英」，助手**应当先反问三件事**（① 品牌名 ② 目标语言与风格 ③ 图片是分好组的一组图还是一整张长图）。**不问就直接开干 = 技能没生效。**

---

## 发布之后

同事只需在 WorkBuddy 里说一句：

> 从这个仓库安装技能：https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill

以后你改了内容，在这个目录里：

```bash
git add -A && git commit -m "更新：xxx" && git push
```

同事说一句「更新一下这个技能」就是最新版，**不用重新发文件**。

---

## 如果同事上不了 GitHub

把仓库换到 **Gitee / 公司内网 GitLab** 就行，流程完全一样，只是 URL 换掉 ——
技能内的脚本不依赖任何域名。或者退回「方式 B：把 zip 拖进对话让助手装」，效果一样。
