# 更新已发布的 skill 到 GitHub（3 步，约 2 分钟）

> **仓库**：https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill （公开，默认分支 `main`）
>
> ⚠️ **不要用 `git push`**。远端历史是当初用 GitHub API 直传推上去的，与本地 git 提交**不相关**，
> 直接 push 会被拒。更新一律走本目录的同步脚本（见第 3 步）。

---

## 第 1 步 · 吊销旧令牌

旧令牌只要**发过别人**、**不确定有没有泄露**、或者**忘了它还剩多久**，就别留着了 —— 吊销比换密码重要，
它等于你账号的一把钥匙。

1. 打开 https://github.com/settings/personal-access-tokens
   （或：右上角头像 → **Settings** → 左下角 **Developer settings** → **Personal access tokens** → **Fine-grained tokens**）
2. 在列表里找到旧令牌（名字通常以 `github_pat_` 开头那串对应的条目）
3. 点右侧 **Delete** → 弹窗确认 **I understand, delete this token**

> 吊销是**立即生效**的：任何还在用它的地方会马上 401。本机没有别的地方在用，可以放心删。

---

## 第 2 步 · 新建令牌（细粒度，只给这一个仓库）

**偷懒直达链接**（能预填大部分字段，剩下两项自己在页面上点）：

```
https://github.com/settings/personal-access-tokens/new?name=howie-skill-sync&description=Sync+intl-cn2en-listing+skill&expires_in=90&contents=write
```

打开后照下表核对/补全：

| 表单字段 | 填什么 | 说明 |
|---|---|---|
| **Token name** | `howie-skill-sync` | 起个好认的名字，将来一眼知道它是干嘛的 |
| **Expiration** | `90 days` | 细粒度令牌**不能永不过期**，最长 366 天 |
| **Resource owner** | `Howie-Linhao`（你自己的账号） | 选组织的话可能要管理员审批 |
| **Repository access** | **Only select repositories** → 勾 `howie-intl-cn2en-listing-skill` | 最小权限；不要选 All repositories |
| **Permissions → Repository permissions** | **Contents: `Read and write`** | ★ 唯一必须给的一项，不给自己改，推送会 403 |
| （Metadata） | `Read-only` | 系统自动带上，不用管 |

最后点 **Generate token**。

> 🔑 令牌**只显示这一次**，关掉页面就再也看不到。当场复制那串 `github_pat_...`。

---

## 第 3 步 · 一条命令同步

```bash
cd "C:\Users\Administrator\WorkBuddy\2026-09-14-13-42-10"
python 同步仓库.py <粘贴你的PAT>
```

脚本会依次做：

1. **镜像** —— 把技能真身（`~/.workbuddy/skills/intl-cn2en-listing/` 的 `SKILL.md` / `references/` / `scripts/`）
   以及工作区的 `安装.py` / `安装说明.md` 覆盖进仓库目录；仓库独有的 `README.md` 等原样保留。
2. **非空校验** —— 0 字节文件（磁盘写坏的信号）直接拦下，不推。
3. **校验令牌** —— 打印令牌属于哪个账号；不是 `Howie-Linhao` 会警告。
4. **直推** —— 走 GitHub Git Data API（blobs → tree → commit → refs），不依赖本地 git、不写本地磁盘。
5. **自检** —— 复查仓库可达、根目录与 `scripts/` `references/` 文件数。

用法补充：

| 命令 | 作用 |
|---|---|
| `python 同步仓库.py --prep` | 只做本地镜像 + 打印差异，**不联网**（推送前想先看看改了哪些文件就用它） |
| `python 同步仓库.py <PAT> --dry` | 镜像 + 校验令牌，**不推送** |

**跑完立刻回第 1 步那个页面，把这个临时 token 也删掉。** 下次要更新再临时领一个就行。

没有本地环境时，也可以直接把 PAT 发给助手代跑。

---

## 发布后必做的一次自检（漏了同事就是白装）

**光有 URL 不等于仓库内容是新的。** 在浏览器打开：

```
https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill
```

- 能打开（200）→ 再看一眼 `scripts/` 里的文件数是否为 **6 个**（`build_bilingual_doc` / `color_caliber` /
  `edsdk_docx` / `glossary_check` / `slice_long_images` / `verify_docx`）；
- **只有 5 个 = `glossary_check.py` 没推上去**，同事的固定文案闸门是空的，错译拦不住；
- 404 → 没发布成功，别发给同事。

---

## 同事装完也要自检（三招）

1. 让他问助手：「列出你本机 `.workbuddy/skills` 目录下有哪些技能」→ 必须看到 `intl-cn2en-listing`；
2. 看路径 `C:\Users\<用户名>\.workbuddy\skills\intl-cn2en-listing\`：**有 `scripts/` 和 `references/` 才算装好**
   （只有一个 `SKILL.md` = 没装好）；
3. 触发一次，说「用国际站速卖通中转英」→ 助手**应当先反问**（① 品牌名 ② 目标语言与风格 ③ 图片是分好组的一组图还是一整张长图）。
   **不问就直接开干 = 技能没生效。**

---

## 常见报错对照

| 报错 | 原因 | 怎么办 |
|---|---|---|
| `401 Bad credentials` | 令牌抄错 / 已被吊销 | 重新领一个 |
| `403 Resource not accessible by personal access token` | 常见两种：① 没给 **Contents: Read and write**；② 想**建仓库**（细粒度令牌没有建仓库的能力，这是 GitHub 的限制，不是权限给少了） | ① 按第 2 步补权限；② 建仓库去 https://github.com/new 网页点，推送仍由脚本做 |
| `404 Not Found`（推已有仓库时） | 仓库名拼错，或令牌的 Repository access 没勾这个仓库 | 核对仓库名与令牌的仓库授权 |
| `409 Git Repository is empty` | 仓库是**全新的空仓库**，Git Data API 用不了 | 先在网页上传一个 `README.md` 造出首个提交，再跑脚本（脚本内已内置这个兜底） |
| `502 / Connection refused`（`git` 相关） | 本机代理会掐掉 `github.com` 的 git/raw 通道 | **别用 git**，脚本走 `api.github.com`，这条通道是通的 |

---

## 同事怎么装

在 WorkBuddy 里说一句：

> 从这个仓库安装技能：https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill

以后仓库更新了，同事让助手「更新一下这个技能」就是最新版，**不用重新发文件**。

---

## 如果同事上不了 GitHub

把仓库换到 **Gitee / 公司内网 GitLab** 就行，流程完全一样，只是 URL 换掉 ——
技能内的脚本不依赖任何域名。或者退回「把 zip 拖进对话让助手装」，效果一样。
