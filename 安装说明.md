# 国际站速卖通中转英 · 安装与使用说明

> **制作者：Howie** ｜ 用起来有问题、想加功能、想改术语，找 Howie。

把中文详情页图片的文案意译成有品牌高级感的英文，输出「中文原图 + 中英对照表」的 Word 文档。
对照表**一行一个句对**（左列中文原文、右列英文文案），逐行可复制上架。

---

## 一、安装（选一种，都不用解压 / 不用手动放目录）

### 方式 A · GitHub 一句话安装（推荐，团队长期用）

仓库：`https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill`

> ⚠️ **发这个链接前，先在浏览器打开一次**。看到仓库页面才算发布成功；
> 显示 404 说明仓库还没建，同事照着装是装不上的（助手取不到技能，只能自由发挥，
> 出来的文档会不符合本技能模板）。

同事在 WorkBuddy 里发一句：

> 从这个仓库安装技能：https://github.com/Howie-Linhao/howie-intl-cn2en-listing-skill

助手会自动 clone 到 `~/.workbuddy/skills/intl-cn2en-listing`。
以后仓库更新了，同事说一句「更新一下这个技能」就是最新版，**不用重新发文件**。

**装完请他自检**：问助手「列出 `.workbuddy/skills` 目录下的技能」，要能看到 `intl-cn2en-listing`；
再触发一次「用国际站速卖通中转英」—— 助手**应当先反问品牌/语言/图片形态三件事**，不问就直接动手说明没装上。

### 方式 B · 把 zip 丢给 WorkBuddy（最省事，微信/钉钉直接发）

同事把 `国际站速卖通中转英-skill.zip` 拖进对话，说一句：

> 帮我安装这个技能包

助手会自己解压、自己放目录、自己补依赖。**同事全程不解压、不拷贝、不改路径。**

### 方式 C · 一键安装器（不用助手时的兜底）

解压后（或 clone 后）在该目录执行任一条：

```bash
python 安装.py                # 自动找到 intl-cn2en-listing 并安装
python 安装.py 技能包.zip      # 直接把 zip 丢给它，自动解压 + 安装
python 安装.py --check        # 只体检，不写文件
```

安装器会自动：定位技能目录 → 复制文件 → 补装 Pillow → 校验 8 个核心文件齐全。

### 装完怎么确认

重开一个对话，直接说「用国际站速卖通中转英」看是否触发。装好的路径形如：

```
C:\Users\<用户名>\.workbuddy\skills\intl-cn2en-listing\SKILL.md
```

> ⚠️ **不要只拿 `SKILL.md` 一个文件**。它只是主流程，真正干活的是同目录下的
> `scripts/`（6 个脚本）和 `references/`（固定文案表、翻译手册、色卡），缺任何一个都会失败。
> 要发给别人，就发那个 **zip**（或仓库地址），不要单发 SKILL.md。

---

## 二、前置条件

| 依赖 | 说明 |
|---|---|
| WorkBuddy 桌面端 | 需要自带内置插件 `tencent-local-office-edit`（脚本会自动搜索安装目录，**不用手改路径**） |
| Python 3 | 脚本自动使用 WorkBuddy 托管 Python；找不到时回落到系统 `python` |
| Pillow | 只在切分长图时需要。**已做首次运行自动补装**，不用手动 `pip install` |

如果脚本报「找不到 tencent-local-office-edit 插件目录」，说明自动搜索没命中，
设一个环境变量即可（指到那个插件的 `skills\tencent-local-office-edit` 目录）：

```
TENCENT_LOCAL_OFFICE_DIR=D:\workbuddy\resources\app.asar.unpacked\resources\plugins\workbuddy-builtin\skills\tencent-local-office-edit
```

---

## 三、怎么用

直接说一句话，附上图片目录：

> 用国际站速卖通中转英，把这个产品的详情页翻一下。图片在 `D:\产品\BD111镍\详情页`，
> 品牌 Aisilan，目标平台国际站 + 速卖通。

助手会先问你几件事（这些是必问项，不是可选项）：

1. **品牌名**？用于替换图内角标署名，如 `designed by Aisilan`（默认 Aisilan）
2. **翻译语言与风格**？默认 **英语（美国）· 简洁 · 高端品牌风格**
3. 图片是**分好组的一组图**，还是**一整张长图**？（两种都行，长图会自动切分）

**两种输入都支持：**
- **分好组的一组图** —— 每张一个模块，最省事、切点最准，推荐；
- **一整张多图整合的长图** —— 自动按图内空白带切成 2-3 段，每段图下跟对应文案。

---

## 四、几条固定口径（品牌规范，会自动执行）

| 项目 | 规则 |
|---|---|
| 材质「碳素钢」 | 统一译 **`Metal`**（不写 Carbon Steel） |
| 颜色名 | 一律查 `references/translation-playbook.md` 第七节色卡，不自由发挥 |
| 同名多译色（砂岩黑） | 按**产品材质表**判定：材质只有金属、无布 → `Matte Black`；材质含亚麻/布艺 → **问用户**；材质缺失 → **问用户**。判不出来时脚本会**中止并提问**，不会自己猜 |
| 图内没有文案的行 | 英文栏一律 `/`，避免被误当成可直接上架的文案 |
| 重复角标 | 只在文首「注」里统一说明一次，正文不逐图重复 |
| 署名 | 图内 `DESIGN BY xxx` 统一替换成确认的品牌署名，并提醒设计端改原图 |
| 尺寸 | 保留原 mm，同时补英寸（`485mm (19.09in)`） |
| 尺寸标注板块 | **一个尺寸只占一行**（不把 225 / 485 / 260 / 25mm 挤在一行，挤在一起分不清引线对哪个数），并带一条固定中文提示：「部位文案仅为定位作用，如原图只有尺寸则忽略该英文文案，只取用 mm 及 inch 尺寸。」 |
| 部位文案（图上引线标签） | 原图自带的那串中文标签**本身就是文案**，要**连贯翻译成英文名词短语**，❌ 不用破折号/斜杠拼接：`碳素钢底座` → `Metal Base`（不是 `Base — Metal`）、`白玉磨砂玻璃` → `Frosted White Jade Glass`；语序按英文习惯「材质 + 部位」，拼写照固定文案表 |
| 排版 | 单元格行距 1.5 倍 + 上下内边距，句与句之间留白；图文设分页保护不被拆散 |

---

## 五、目录结构

```
intl-cn2en-listing/
├── SKILL.md                              主流程（排版原则 / 踩坑清单 / 自检项）
├── scripts/
│   ├── build_bilingual_doc.py            主生成器：读 job.json 一次生成整份文档（尺寸块自动拆行 + 带中文提示）
│   ├── color_caliber.py                  颜色口径闸门（同名多译色按材质判定）
│   ├── glossary_check.py                 固定文案口径闸门（客户指定译法 + 违禁变体）
│   ├── slice_long_images.py              长图切分（自动 / 指定切点两种模式；Pillow 自动补装）
│   ├── verify_docx.py                    交付前校验（不依赖渲染服务；含尺寸行数 / 提示段数）
│   ├── edsdk_docx.py                     editor_sdk 调用封装（路径自动探测）
│   └── job.example.json                  作业文件示例，照它填
└── references/
    └── translation-playbook.md           第〇节固定文案表 / 意译原则 / 术语表 / 色卡 / 单位换算 / 自检清单

安装.py                                   一键安装器（可选；方式 A/B 用不到）
```

---

## 六、常见问题

| 现象 | 处理 |
|---|---|
| 脚本报找不到插件目录 | 见上面「前置条件」设 `TENCENT_LOCAL_OFFICE_DIR` |
| 切长图报 `No module named 'PIL'` | 已做自动补装；仍失败就手动 `python -m pip install pillow` |
| 文档已开着，生成时写入失败 | 先关掉预览再重跑；脚本自身也有「另存 + 重命名」兜底 |
| 颜色闸门中止并提问 | 这是**正常设计**：材质含布或材质缺失时不能替用户选色号，回答后写进 `job.json` 的 `material` 再重跑 |
| 想确认产出是否合格 | `python scripts/verify_docx.py <输出.docx> <块数> <句对数>`，输出 `[PASS]` 即合格 |
| 同事装完不生效 | 确认装到了 `~/.workbuddy/skills/intl-cn2en-listing/SKILL.md`（不是只放了 SKILL.md），然后**重开对话** |

---

有任何改不动的口径（比如你们另有材质/颜色规范），直接改
`references/translation-playbook.md` 与 `SKILL.md`，两处都是纯文本。
